<h1> All posts </h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <% if !logged_in? %>
        Someone wrote
      <% else %>
        <%= post.author %> wrote
      <% end %>
      <br>
      <%= post.comment %>
    </li>

  <% end %>
</ul>
